class CartItem {
    constructor(quantity, productTitle, productPrice, sum, pushToken) {
        this.quantity = quantity
        this.productTitle = productTitle
        this.productPrice = productPrice
        this.sum = sum
        this.pushToken = pushToken
    }
}

export default CartItem
